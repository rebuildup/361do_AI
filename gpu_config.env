# GPU最適化設定ファイル
# RTX 4050 (6GB VRAM) 向け最適化設定

# 基本OLLAMA設定
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=qwen2:7b-instruct
OLLAMA_TIMEOUT=60

# GPU高速化設定
OLLAMA_GPU_ENABLED=true
OLLAMA_GPU_MEMORY_FRACTION=0.7  # RTX 4050の場合は70%が安全
OLLAMA_GPU_LAYERS=28             # RTX 4050に最適化
OLLAMA_PARALLEL_REQUESTS=2       # 6GB VRAMでは2並列が最適

# エージェント設定
DATABASE_URL=sqlite:///data/agent.db
AGENT_LEARNING_ENABLED=true
AGENT_LEARNING_INTERVAL_MINUTES=30

# パフォーマンス最適化
AGENT_USE_CODEX_AGENT=true
CODEX_CWD=.

# 使用方法:
# Windows: 
#   set /p < gpu_config.env
# Linux/Mac:
#   source gpu_config.env